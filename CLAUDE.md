# CLAUDE.md — ESG Report Automation (ESPRIT Project #45)

Context file for Claude Code. Read this first; it captures decisions and
hard-won gotchas from the earlier build sessions.

---

## 1. The project

**What:** An automated ESG reporting system for universities.
Pipeline: scrape public sustainability data → structure it → map it onto
international reporting frameworks → generate a report with an LLM → feed a BI
dashboard.

**Who:** Hakim, 4th-year Data Science engineering student, ESPRIT School of
Engineering (Tunisia). Supervisor: Hiba Maalaoui.

**Deliverables must be in English** (reference documentation from the supervisor
is in French — do not let French leak into outputs).

**Target frameworks:** GRI, ISSB, CSRD/ESRS, TCFD, SASB.

---

## 2. Architecture — 6 phases

| # | Phase | Status |
|---|-------|--------|
| 1 | **Knowledge** — scrape ESG reference sites → RAG corpus | ✅ done |
| 2 | **Extraction** — scrape STARS reports (scores + detail) | ✅ done |
| 3 | **Structure** — combine into one schema, tag E/S/G pillars | ✅ done |
| 4 | **Map** — STARS credits → GRI / TCFD / ESRS disclosures | ⬅ **next** |
| 5 | **Generate** — LLM writes prose, code injects numbers | pending |
| 6 | **Output** — ESG report + BI dashboard | pending |

### Two input branches (important conceptual split)

- **Branch A — Knowledge:** reference websites (IBM, GRI, CSRD, SASB, STARS…).
  Teaches the LLM the *language* of ESG. No numbers. Feeds RAG.
- **Branch B — Data:** AASHE STARS reports for 3 universities. The *facts* the
  report is about. All numbers.

One-liner: *"Knowledge grounds how the model writes; data is what it writes about."*

---

## 3. NON-NEGOTIABLE design decision — numbers safety

**The LLM never touches a number it could alter.**

- LLM writes narrative containing placeholders: `"GHG emissions scored {OP6} of {OP6_MAX}"`
- **Code** (Jinja2 template engine) substitutes the real value: `{OP6}` → `10.63`

Phrase used with the supervisor: **"non-hallucinable by construction."**

Corollary: the **framework mapping table is built by hand against the real
standards — never generated by an LLM.** Fabricated mappings would be an
integrity failure in an ESG tool.

---

## 4. Data sources — the three universities

All three use **STARS 3.0**, so one parser works for all.

| Institution | Rating | Score | Report date (URL segment) |
|---|---|---|---|
| UC Berkeley | Platinum | 86.76 | `2025-02-19` |
| University College Cork (IE) | Platinum | 85.99 | `2026-03-05` |
| Technological University Dublin (IE) | Gold | 83.35 | `2024-12-02` |

Base URL pattern:
```
https://reports.aashe.org/institutions/{slug}/report/{DATE}/{CAT}/{subcat}/{CREDIT}/
```
Slugs: `university-of-california-berkeley-ca`,
`university-college-cork-national-university-of-ireland-cork-co-corcaigh`,
`technological-university-dublin-dublin`

### Why these three (defensible reasoning — keep it)

- **AASHE STARS** is the closest thing to a standard for university ESG
  (416 rated institutions). Corporate ESG has referees (MSCI, S&P); universities
  self-report. That gap is the problem this project attacks.
- **Berkeley** chosen for a complete **G pillar** — it publishes investment
  policy and holdings (PA-4, PA-5), which most universities don't.
- **Europe requested by supervisor** (European system closer to Tunisian).
  Checked all 416 rated institutions: **none of the supervisor's 10 European
  candidates are in STARS** (Manchester, Imperial, Lund, Uppsala, TU Delft,
  Edinburgh, Bristol, ETH Zurich, Glasgow, Aix-Marseille). Only 8 European
  institutions have STARS ratings; **Ireland has more than the rest of Europe
  combined.** Hence Cork + TU Dublin.
- **The Toronto trap:** Toronto is QS #1 most sustainable university (2024,
  2025) but its STARS data exists **only for the Mississauga satellite campus** —
  the main St. George campus never submitted. Reputation ≠ data.
- **UBC eliminated:** last STARS report 2015, expired 2018.
- **France considered and rejected:** the French **Label DD&RS** framework is
  conceptually closest to the Tunisian system, but publishes *labels, not data* —
  no per-institution scorecards. Good context, unusable as a data source.

---

## 5. Files in the repo

### Scrapers — Branch B (STARS data), two levels each

**Level 1 — scorecard** (public, no auth): one score per credit.
- `scrape_berkeley.py` → `berkeley_stars.csv`
- `scrape_cork.py` → `cork_stars.csv`
- `scrape_tudublin.py` → `tudublin_stars.csv`

**Level 2 — credit detail** (**requires auth**): Q&A + narrative per credit.
- `scrape_berkeley_deep_v2.py` → `berkeley_credits.txt`, `berkeley_qa.csv`
- `scrape_cork_deep_v2.py` → `cork_credits.txt`, `cork_qa.csv`
- `scrape_tudublin_deep_v2.py` → `tudublin_credits.txt`, `tudublin_qa.csv`

> Use the `_v2` deep scripts only. v1 had no login detection and silently wrote
> 118 pages of "Please log in" placeholder text.

### Scraper — Branch A (knowledge)
- `scrape_knowledge_v2.py` → `knowledge_sources/*.txt` + `_summary.csv`

### Phase 3
- `combine_universities.py` → `combined_esg_dataset.csv`

---

## 6. GOTCHAS — read before running anything

### 6.1 STARS credit-detail pages require a login
Scorecard pages are public. **Credit detail pages are not.** AASHE accounts are
**free to anyone** (not members-only).

Auth is via session cookie in an **environment variable — never hard-code it**:
```bash
export AASHE_SESSIONID="<sessionid cookie value from browser devtools>"
```
The `_v2` deep scrapers run a **preflight**: they test one page and abort with
instructions if blocked, instead of wasting 118 requests.

### 6.2 Poisoned cache
If a run was blocked, the cache holds login pages. Delete before retrying:
```bash
rm -rf cache_credits cache_credits_cork cache_credits_tudublin
```
The `_v2` scripts auto-detect and drop poisoned cache entries, but a manual
clear is safest.

### 6.3 Encoding / mojibake
`requests` defaults to latin-1 and mangles UTF-8 (`617,090â`). Always:
```python
if not resp.encoding or resp.encoding.lower() == "iso-8859-1":
    resp.encoding = resp.apparent_encoding
```

### 6.4 Cross-version comparability — REAL METHODOLOGY BREAK
STARS **v2.2 and earlier** scored Operations credits (water, energy, GHG, waste —
the whole E pillar) **against the institution's own baseline** (improvement over
time). **v3.0 switched to scoring against a peer group** (Associate /
Baccalaureate / Masters / Doctoral thresholds).

**A naive year-over-year time series across versions is invalid.** AASHE
publishes a "Summary of Changes" doc in the Technical Manual. All three current
reports are v3.0, so *current* comparisons are fine — the issue only arises when
pulling a university's older reports.

### 6.5 Deep-page data mostly lands as "narrative", not clean Q&A
Berkeley deep run: 118 credits, ~354 Q&A rows, narrative on every credit.
Most Q&A pairs are the repeated header (Overall Rating / Score / Date).
Reason: STARS credit tables are **not simple two-column label/value** — the
parser's Q&A logic only catches clean 2-cell rows, so real figures fall into the
narrative catch-all. **Nothing is lost**, but exact numbers are prose, not rows.

Whether this needs fixing is an **open question for the supervisor** (see §9).

### 6.6 TU Dublin has no investment data — this is real, not a bug
`PA-4` (Sustainable Investment Program) and `PA-5` (Investment Holdings) come
back **"Not Applicable"**. TU Dublin is a public technological university with no
endowment. Keep these rows (don't drop them) — it's a genuine finding about
applying corporate-derived ESG frameworks to the higher-ed sector.

### 6.7 AASHE data use terms
STARS data is **not** openly licensed (unlike World Bank CC BY 4.0). It is
publicly accessible and usable in research **with attribution to AASHE**.
Do not describe it as "open licensed". Citation format:
> [Institution] STARS Report. AASHE. [Date published]. Retrieved from [URL].

Politeness: 1.0–1.5s pause between requests, cache everything locally.

---

## 7. Current results

**Berkeley scorecard:** 70 credits, 66 scored (the 4 unscored are `PRE-*`
narrative credits — correct, not a bug). Core AC/EN/OP/PA = 206.91 / 250.
21 IL credits are **bonus** (capped at 10 in reality) — tag separately so they
don't inflate pillar totals.

**Berkeley deep:** 118 credit pages, ~381 KB, 354 Q&A rows + narrative.

**Knowledge corpus:** 18/20 sources clean, **21,654 words, 13/14 English**.
Two known failures, both already covered by other files:
- `gri-standards` (globalreporting.org is JS-heavy) → covered by `ibm-gri` (1,037w)
- `stars-terminology` (returned 0) → covered by `stars-technical-manual`, which
  contains the **full STARS 3.0 credit inventory** (OP 1–15, PA 1–13)
Known flags: `greenscope-agencies` is FRENCH (correctly labelled — it is a French
consultancy, **not S&P**, do not relabel it as S&P).

---

## 8. Phase 3 pillar mapping (implemented in `combine_universities.py`)

```
OP           → Environmental
PA-1..PA-5   → Governance   (coordination, planning, governance, investment)
PA-6..PA-13  → Social       (climate, representation, wellbeing, pay equity)
AC / EN      → Context      (ESG-relevant, not a corporate pillar)
IL           → Bonus        (capped bonus points)
PRE          → Preface      (unscored)
```
This is a **reasoned grouping, not an official STARS designation** — STARS ships
no E/S/G labels. Adjustable via one dict if the supervisor prefers another
convention.

Output columns: `institution, stars_version, pillar, category, category_code,
credit_code, credit_name, status, score, max`

---

## 9. OPEN QUESTIONS for the supervisor — resolve before building Phase 4/5

1. **Does the BI dashboard need the deep inner-page figures (actual tonnes CO₂,
   kWh), or just the STARS scores?**
   - Scores only → extraction is complete; deep data stays as RAG context.
   - Raw figures → the deep parser needs upgrading (see §6.5).
2. **Framework priority.** The brief lists five (GRI/ISSB/CSRD-ESRS/TCFD/SASB).
   Mapping to all five is a lot of manual work. Is GRI the priority?

Do not guess these. They change what gets built.

---

## 10. Phase 4 — how the mapping works (next task)

**The problem:** STARS speaks its own vocabulary (`OP-6`, `PA-13`). Reports must
speak GRI/ESRS. Mapping is the translation table.

**Structural difference:**

| | STARS | GRI |
|---|---|---|
| Built for | Universities | Any organization |
| Purpose | Score you (Bronze→Platinum) | Standardize disclosure |
| Unit | **Credit** (worth points) | **Disclosure** (no points) |
| Structure | Categories → Credits | Universal / Sector / Topic → Disclosures |

*"STARS grades you; GRI standardizes what you tell people."*

**Shape of the deliverable** (a hand-built lookup dict/JSON):
```
OP-6  Greenhouse Gas Emissions   → GRI 305 (Emissions)      → ESRS E1 (Climate)
OP-3  Water Use                  → GRI 303 (Water)          → ESRS E3 (Water)
PA-13 Pay Equity & Living Wage   → GRI 405 (Diversity)      → ESRS S1 (Own Workforce)
PA-11 Health, Safety & Wellbeing → GRI 403 (Occupational H&S) → ESRS S1
```
⚠️ **The rows above are illustrative shape, NOT verified mappings.** Build the
real table against the actual GRI/ESRS documents and cite them.

**Three complications to expect:**
1. **Many-to-many** — one credit can feed several disclosures and vice versa.
2. **Gaps both ways** — GRI asks for things STARS doesn't collect; STARS collects
   things GRI has no slot for (e.g. AC credits = sustainability courses taught —
   corporations don't teach classes).
3. **The gaps are a research finding, not a failure.** "Here is exactly what a
   university can and cannot report under GRI" is likely the most original
   contribution of the report.

The Phase 3 `pillar` column is the **first half of this mapping already done** —
GRI topics group into environmental (300s), social (400s), governance.

---

## 11. Tech stack

**In use (extraction):**
`requests` (fetch) · `requests.Session` (holds the auth cookie) ·
`BeautifulSoup` + `lxml` (precise field extraction — STARS) ·
`trafilatura` (clean article text — knowledge sources) · `pandas` (tables/CSV) ·
`re` · `pathlib` · `os` (env vars for secrets) · `time` (polite pauses)

Why two HTML readers: **BeautifulSoup for data** (want exact fields — tweezers),
**trafilatura for knowledge** (want clean prose, discard structure — strainer).

**Planned:** Jinja2 (number injection) · Claude API (narrative) · LangChain +
ChromaDB + sentence-transformers (RAG) · SQLite (Phase 3+ store) ·
python-docx / WeasyPrint (report) · Power BI or Streamlit + Plotly (dashboard)

**Why files not a database (yet):** extraction output is raw and the schema is
still settling; files are inspectable. The database belongs in the Structure
phase once the shape is locked. This is deliberate sequencing, not a shortcut.

---

## 12. Working principles established

- **Verify, don't recall.** Fetch/search before asserting facts, URLs, or
  standards. Several earlier errors came from asserting from memory.
- **Flag problems honestly** rather than papering over them — the supervisor
  values named limitations (they were put on the slides deliberately).
- **Match the tool to the page.** Static HTML → `requests`. Only use Playwright
  where JS rendering is genuinely required.
- **Fail loudly, never silently.** The worst bug so far was a scraper that saved
  118 blocked pages without noticing.
- **Scope before building.** Don't build a parser upgrade for data nobody has
  confirmed they need.
